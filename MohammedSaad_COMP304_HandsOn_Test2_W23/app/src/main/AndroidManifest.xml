<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mohammed.mohammedsaad">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MohammedSaad_COMP304_HandsOn_Test2_W23"
        tools:targetApi="31">

        <!-- Declare the Main Activity -->
        <activity
            android:name=".MohammedActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MohammedSaad_COMP304_HandsOn_Test2_W23">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare the Broadcast Receiver -->
        <receiver android:name=".StockBroadcastReceiver">
            <intent-filter>
                <action android:name="com.mohammed.mohammedsaad.STOCK_INFO_BROADCAST" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
